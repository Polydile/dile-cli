import { LitElement, html, css } from 'lit';
import { DileForm } from '@dile/ui/mixins/form'

import '@dile/ui/components/input/input.js';
import '@dile/ui/components/password/password.js';


export class userLoginForm extends DileForm(LitElement) {
  static styles = [
    css`
      :host {
        display: block;
      }
    `
  ];


  render() {
    return html`
      <dile-input label="Email" name="email" id="email" hideErrorOnInput></dile-input>
      <dile-password label="Password" name="password" id="password" hideErrorOnInput></dile-password>
    `;
  }
}
customElements.define('user-login-form', userLoginForm);
