import { LitElement, html, css } from 'lit';
import '@dile/crud/components/single/crud-single'
import { {{properCamelCase entity}}Config } from '{{configImportPath}}';

export class {{properPascalCase entity}}Single extends LitElement {
  static styles = [
    css`
      :host {
        display: block;
      }
    `
  ];

  static get properties() {
    return {
      config: { type: Object },
      relatedId: { type: String },
      language: { type: String },
    };
  }

  constructor() {
    super();
    this.config = {{properCamelCase entity}}Config.getConfig();
  }

  render() {
    return html`
      <dile-crud-single
        relatedId="${this.relatedId}"
        .config=${this.config}
        language="${this.language}"
      ></dile-crud-single>
    `;
  }
}
customElements.define('{{kebabCase name}}', {{properPascalCase entity}}Single);
